server {
    listen 80;
    server_name _;

    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;
    gzip off;

    # --- CORS (inherited by every location below) ---
    add_header Access-Control-Allow-Origin '*' always;
    add_header Access-Control-Allow-Methods 'GET, HEAD, OPTIONS' always;
    add_header Access-Control-Allow-Headers 'Range' always;
    add_header Access-Control-Expose-Headers 'Content-Range, Accept-Ranges, Content-Length' always;

    # --- Data folder that lives in the repo ---
    location / {
        root /home/manulinux/stac-example/data;

        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin '*' always;
            add_header Access-Control-Allow-Methods 'GET, HEAD, OPTIONS' always;
            add_header Access-Control-Allow-Headers 'Range' always;
            add_header Content-Length 0;
            add_header Content-Type text/plain;
            return 204;
        }
    }

    # --- Extra folder outside the repo ---
    location /extra-data/ {
        alias /home/manulinux/Files_Voxel_CNN/;

        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin '*' always;
            add_header Access-Control-Allow-Methods 'GET, HEAD, OPTIONS' always;
            add_header Access-Control-Allow-Headers 'Range' always;
            add_header Content-Length 0;
            add_header Content-Type text/plain;
            return 204;
        }
    }
}
